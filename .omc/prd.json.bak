{
  "title": "Agent Skills Integration for heare",
  "description": "Integrate agentskills.io format skills into heare's Pipecat voice pipeline",
  "stories": [
    {
      "id": "US-001",
      "title": "Delete orphaned src/skills.py",
      "description": "Remove the broken, never-used src/skills.py module that has zero imports and a broken _execute_skill_logic implementation",
      "acceptanceCriteria": [
        "File /Users/lenyk/myprojects/heare/src/skills.py is deleted",
        "grep -r 'from src.skills import' /Users/lenyk/myprojects/heare/src /Users/lenyk/myprojects/heare/tests returns zero results",
        "grep -r 'from .skills import' /Users/lenyk/myprojects/heare/src /Users/lenyk/myprojects/heare/tests returns zero results"
      ],
      "passes": true
    },
    {
      "id": "US-002",
      "title": "Create src/agent_skills.py with SkillsLoader",
      "description": "Implement SkillMetadata dataclass and SkillsLoader class with discover(), load_instructions(), and get_skill_names() methods, plus module-level singleton",
      "acceptanceCriteria": [
        "File /Users/lenyk/myprojects/heare/src/agent_skills.py exists",
        "SkillMetadata dataclass has fields: name (str), description (str), path (Path)",
        "SkillsLoader class has __init__(search_paths: list[Path])",
        "SkillsLoader.discover() returns list[SkillMetadata] with no bodies loaded",
        "SkillsLoader.load_instructions(name: str) returns full SKILL.md body as string",
        "SkillsLoader.get_skill_names() returns list[str] of skill names",
        "get_skills_loader(settings) singleton function exists and caches loader instance",
        "Malformed SKILL.md files are skipped with WARNING-level log message",
        "Module imports logging, dataclasses, Path, re (for YAML parsing)"
      ],
      "passes": false
    },
    {
      "id": "US-003",
      "title": "Add skills_paths to src/config.py",
      "description": "Add skills_paths field to Settings dataclass with default value ['~/.heare/skills']",
      "acceptanceCriteria": [
        "Settings dataclass in src/config.py has field: skills_paths: list[str] = field(default_factory=lambda: ['~/.heare/skills'])",
        "load_settings() function correctly reads skills_paths from TOML config file if present",
        "skills_paths defaults to ['~/.heare/skills'] when not in config"
      ],
      "passes": false
    },
    {
      "id": "US-004",
      "title": "Register list_skills and run_skill tools",
      "description": "Add both meta-tools to TOOLS dict in tool_registry.py, add _TOOL_SPECS entries in llm_tools.py, and add cases in direct_tools.py execute_direct() switch",
      "acceptanceCriteria": [
        "TOOLS dict in src/tool_registry.py has 'list_skills': Tool(...) with execution='direct'",
        "TOOLS dict has 'run_skill': Tool(...) with execution='direct'",
        "_TOOL_SPECS in src/llm_tools.py has 'list_skills': ({}, [], callable)",
        "_TOOL_SPECS has 'run_skill': ({name, context props}, [name, context required], callable serializer)",
        "execute_direct() in src/direct_tools.py has case 'list_skills' that returns available skills",
        "execute_direct() has case 'run_skill' that parses args and dispatches to _execute_skill_internal()",
        "list_skills case returns {success: true, output: formatted_names, spoken: {...}}",
        "run_skill case returns error with valid skill names list when skill not found"
      ],
      "passes": false
    },
    {
      "id": "US-005",
      "title": "Implement _execute_skill_internal() and skill execution logic",
      "description": "Implement async _execute_skill_internal() helper that executes skill instructions via execute_direct(), with single action-log entry semantics and timeout handling",
      "acceptanceCriteria": [
        "Function _execute_skill_internal(skill_name, instructions, context, settings, action_timeout_seconds) exists in src/direct_tools.py",
        "Function is async and respects asyncio.CancelledError for interruption",
        "Calls execute_direct() for each tool in the skill without passing conversation_manager (prevents duplicate action-log entries)",
        "Returns composite result dict with {success: bool, output: str, spoken: {lang: str}}",
        "Enforces per-skill timeout using action_timeout_seconds parameter",
        "Function _extract_tool_calls_from_instructions(instructions, context) exists and extracts tool calls via regex",
        "Extracted tool names match existing heare tools (bash, read, write, web_search, etc.)",
        "Context variables in instructions are substituted correctly"
      ],
      "passes": false
    },
    {
      "id": "US-006",
      "title": "Inject skill names in system prompt and write test suite",
      "description": "Add brief skill names section to render_native_system_prompt() in llm_context_injector.py, and write all 15 test cases in tests/test_agent_skills.py",
      "acceptanceCriteria": [
        "render_native_system_prompt() in src/llm_context_injector.py calls get_skills_loader() and injects brief skill names (~50 tokens max for 10 skills)",
        "System prompt includes 'Available Skills' section with skill names when skills are present",
        "System prompt omits skills section when no skills are installed",
        "tests/test_agent_skills.py exists with all 15 test cases (unit, integration, E2E, observability)",
        "make test returns success with all tests passing",
        "make lint returns success with no ruff errors"
      ],
      "passes": false
    }
  ],
  "metadata": {
    "createdAt": "2026-05-01T16:26:00Z",
    "iteration": 1,
    "maxIterations": 5
  }
}
