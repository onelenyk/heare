# PRD WU — Conversation Core Wireup

## Sprint kickoff (2026-04-25)
- Source plan: .omc/plans/conversation-core-wireup.md (consensus-approved via ralplan iteration 0)
- Branch: s2s-realtime, layer commits on 16eba0e (the legacy claude_cli removal commit)
- 6 stories, ~21 story points
- 4 consensus execution conditions baked into prd.json:
  - EC-1: US-WU-01 INTENT_CANCELLED fires exactly once at actions.py:233; generator's manual notify block deleted
  - EC-2: US-WU-02 check_cancel + CANCEL_PATTERNS MUST be deleted (no escape hatch)
  - EC-3: US-WU-05 create_decider_processor raises RuntimeError; sibling tests pass naturally OR are skip-marked
  - EC-4: US-WU-04 deletes config.py:401-406 env override; PR body calls out HEARE_HEARTBEAT_MIN removal

## Story order (justified in plan Section C)
1. US-WU-06 — wallclock pin (1 line, ships first to clean CI signal)
2. US-WU-01 — replace cancel_latest with cancel_active (CCS-05b kill paths reach prod)
3. US-WU-02 — promote _is_standalone_cancel_imperative + delete check_cancel (CCS-05a smart detection reaches prod)
4. US-WU-04 — remove HeartbeatTask + delete env override
5. US-WU-03 — delete CCS-03 dead code (Option A)
6. US-WU-05 — delete tests/test_decider.py + most of decider.py (highest-risk deletion last)

## US-WU-01 — implemented (2026-04-25)
- One-line swap: src/generator.py:404 now calls `await self.intent_queue.cancel_active()`
  in place of the synchronous `self.intent_queue.cancel_latest()`. The cancel keyword
  gate now reaches `ActionWorker.cancel_in_flight` (CCS-05b kill paths).
- Deleted notify block: src/generator.py:408-415 manual `Indication.notify(IntentKind.INTENT_CANCELLED, ...)`
  fully removed. Single emit site is `src/actions.py:233` inside `IntentQueue.cancel_active`,
  satisfying EC-1.
- Renamed `cancelled_id` log field: the `[TIMING] generator ... cancelled=...` line at
  src/generator.py:500-508 now emits the literal "yes" / "none" derived from
  `cancelled_anything: bool`; the int-id field was dropped. Added a new
  `[CANCEL FAST-PATH transcript=...]` info log when cancel_active returns True.
- Existing test `test_cancel_keyword_pops_pending_intent` updated to assert the new
  `[CANCEL FAST-PATH]` marker instead of the deleted `[INTENT CANCELLED id=N]` line.
- Three new tests in tests/test_generator.py exercise EC-1:
  * cancel_active is awaited exactly once and the generator does NOT call
    Indication.notify(INTENT_CANCELLED) itself.
  * empty-queue / no-in-flight cancel produces zero INTENT_CANCELLED notifications.
  * cancel keyword still lets the LLM run for a spoken acknowledgement.
- `cancel_latest` callers still in tree (intentional, per plan "Don't change"):
  * src/actions.py:137 — definition (kept; may have other callers later)
  * tests/test_actions.py:36,40,48,50,59 — direct unit tests of cancel_latest itself
  * tests/test_indication_wiring.py:56 — fixture mock for an unrelated test
  No production call site of `cancel_latest` remains in src/.
- Verification: `uv run pytest tests/test_generator.py tests/test_actions.py
  tests/test_intent_parser.py tests/test_indication.py -q` → 115 passed.
  Full suite `uv run pytest -q` → 994 passed, 4 pre-existing warnings.
  `uv run ruff check src/generator.py tests/test_generator.py` → all checks passed.

## US-WU-02 — implemented (2026-04-25)
- Function move: `_is_standalone_cancel_imperative` promoted from
  src/decider.py:229 to src/language.py as the public
  `is_standalone_cancel_imperative(text, stop_words)` (no leading underscore).
  Same signature, same logic — ≤4-word residual, negation guard
  ("don't stop", "не зупиняйся" rejected), context-token guard
  ("stop sign", "the cancel button" rejected), "no no no stop" carve-out.
  Private constants (_CANCEL_POLITENESS_PREFIXES, _CANCEL_FILLER_PREFIXES,
  _CANCEL_NEGATION_TOKENS, _CANCEL_CONTEXT_TOKENS, _CANCEL_MAX_RESIDUAL_WORDS)
  moved with it.
- EC-2 deletions: `check_cancel` function and the `CANCEL_PATTERNS` regex
  dict are GONE from src/language.py. `grep -rn 'check_cancel\|CANCEL_PATTERNS'
  src/` returns zero matches. The keyword-list approach is gone — no escape
  hatch remains.
- Generator wired: src/generator.py:403 cancel keyword gate now calls
  `is_standalone_cancel_imperative(transcript, settings.cancel_stop_words)`.
  CCS-05a smart detection finally reaches production after the post-mortem
  found the original CCS-05a code lived in dormant DeciderProcessor. Falls
  back to the default stop-word list when settings is None (test paths).
- Decider import updated: src/decider.py imports
  `is_standalone_cancel_imperative` from src.language and the dormant
  fast-path call site at line ~598 uses the public name. Sibling
  consistency for US-WU-05.
- Test migration: tests/test_language.py grew a fixture-driven parametrized
  test pair (`test_is_standalone_cancel_imperative_fixtures_trigger` /
  `..._no_trigger`) that loads tests/fixtures/cancel_stopwords.txt and
  asserts every TRIGGER row → True and every NO-TRIGGER row → False with
  the default stop-word list. The legacy `test_check_cancel_*` tests were
  deleted (the function they tested no longer exists). The
  `test_cancel_stop_words_extensible_via_env` env-override test moved
  here too.
- Test phrase updates in tests/test_generator.py: the cancel-path tests
  previously used "скасуй" — that token is NOT in the canonical
  stop-word list ("stop", "cancel", "halt", "відміни", "отмени", "стоп")
  and the smart detector intentionally requires the head token to match
  exactly. Phrases swapped to detector-recognized stop-words: "стоп",
  "стоп!", "будь ласка стоп", "відміни замовлення", "ok stop",
  "please cancel". `test_cancel_keyword_negative_cases` ("стоп-кадр",
  "автостоп", "скаси мене") still passes — the smart detector treats
  hyphenated/concatenated tokens as a single residual word that does
  NOT match the standalone "стоп".
- tests/test_decider.py: the fixture-driven detector tests
  `test_standalone_cancel_imperative_*` were migrated out (now in
  tests/test_language.py). The dormant-DeciderProcessor wrapper tests
  (`test_decider_fast_path_*`, `test_cancel_stop_words_extensible_via_env`)
  remain but now import from src.language. They will be removed in
  US-WU-05 with the rest of the dormant decider tests.
- Verification:
  * `uv run pytest tests/test_language.py tests/test_generator.py
    tests/test_decider.py -q` → 179 passed.
  * Full suite `uv run pytest -q` → 991 passed, 4 pre-existing warnings.
  * `uv run ruff check src/ tests/` → all checks passed.
  * `grep -rn 'check_cancel\|CANCEL_PATTERNS' src/` → zero matches (EC-2).

## US-WU-04 — implemented (2026-04-25)
- Removed dead HeartbeatTask code path. The legacy ticker fired every N
  minutes and called into `GeneratorProcessor.on_heartbeat_tick` — a
  no-op stub left over from the pre-S2S architecture. No proactive
  speech in production, no user demand. Whole path deleted including
  the env override.
- File-level changes:
  * `src/heartbeat.py`: `HeartbeatTask` class deleted. File kept (it
    still hosts `WarmupTask`, which is unrelated — pings edge-tts every
    240s to keep the WSS warm). Module docstring rewritten to record
    the removal.
  * `src/main.py`: import shrunk to `from .heartbeat import WarmupTask`;
    `heartbeat = HeartbeatTask(...)` construction removed; the
    `heartbeat=heartbeat` argument dropped from the `run_until_stopped`
    call. `run_until_stopped` itself loses its `heartbeat` positional
    parameter — the `heartbeat_task = loop.create_task(heartbeat.run())`
    line, the `watch_set` membership, the `heartbeat.stop()` call, the
    `(heartbeat_task, "heartbeat")` named-task entry, and the
    `background_tasks` membership all gone.
  * `src/generator.py`: the no-op `on_heartbeat_tick` stub at
    src/generator.py:569-571 deleted. `shutdown()` retained for main
    teardown parity. No other production caller of the stub remained.
  * `src/config.py`: `heartbeat_interval_minutes: int = 30` Setting
    field deleted (line 147). The `HEARE_HEARTBEAT_MIN` env override
    block at lines 401-406 deleted (EC-4 from consensus). Both go
    together — no orphan parse-without-field, no orphan field-without-
    parse.
  * `README.md`: the "Parallel: HeartbeatTask every N minutes ..."
    diagram annotation, the "and heartbeat check-ins" mode-table cell
    fragment, and the "Heartbeat ticks are suppressed while heare is
    waiting for your confirmation" paragraph all removed. The SQLite
    schema-comment line that lists "heartbeats" as a table is RETAINED
    on purpose: the `heartbeats` table itself still lives in
    `src/storage.py` (created via `CREATE TABLE IF NOT EXISTS
    heartbeats`) and is touched by the dormant decider's
    `log_heartbeat()` call. The DB schema is out of scope for this
    story — US-WU-05 deletes the dormant decider and the schema cleanup
    can ride alongside if desired.
- What was kept (intentional):
  * `WarmupTask` (different lifecycle, real production work — keeps
    edge-tts WSS warm).
  * `IndicationKind.HEARTBEAT_TICK` enum value in `src/indication.py`
    (plus its level-map and message-map entries). Per plan open-question
    deferral: leaving it costs nothing, three indication tests still
    reference it.
  * `DeciderProcessor.on_heartbeat_tick` at `src/decider.py:1164` and
    `store.log_heartbeat` in `src/storage.py`. The dormant decider is
    no longer instantiated by `_cmd_start` (since 16eba0e), so this
    `on_heartbeat_tick` is reachable ONLY from `tests/test_decider.py`.
    US-WU-05 (next story) deletes `tests/test_decider.py` and most of
    `src/decider.py` — heartbeat code rides along then.
- Test changes:
  * `tests/test_generator.py`: `test_on_heartbeat_tick_is_noop` deleted
    (covered the removed stub).
  * `tests/test_config.py`: `test_load_settings_heartbeat_override` and
    `test_load_settings_heartbeat_invalid` deleted (covered the removed
    env block). The `heartbeat_interval_minutes == 30` assertion in
    `test_default_settings` removed. The `monkeypatch.delenv(
    "HEARE_HEARTBEAT_MIN", raising=False)` line in
    `test_deprecated_enable_mcp_servers_warning` removed (env var no
    longer parsed, so leakage between tests is impossible).
  * `tests/test_shutdown.py`: rewritten — the `SleepingHeartbeat` shim
    class deleted; all four `run_until_stopped(...)` calls drop the
    heartbeat parameter; the heartbeat-related assertions
    (`heartbeat.cancelled`, `heartbeat.stopped`) removed. The
    `test_run_until_stopped_cancels_when_pipeline_exits` assertion that
    used `heartbeat.cancelled or heartbeat.stopped` is gone — the test
    just verifies the call returns within the timeout (the pipeline
    exit path is what matters).
  * `tests/integration/test_s2s_pipeline.py`: dropped the
    `assert callable(getattr(middle, "on_heartbeat_tick", None))` check
    (the contract no longer exists).
- EC-4 evidence (consensus deployment-impact requirement):
  * `grep -rn 'HEARE_HEARTBEAT_MIN' src/ tests/` → zero matches.
  * `grep -rn 'HeartbeatTask\|heartbeat_interval_minutes' src/` → zero
    matches.
  * `grep -rn 'on_heartbeat_tick' src/` → exactly one match:
    `src/decider.py:1164` (dormant decider, no production caller; queued
    for deletion in US-WU-05).
- Verification:
  * `uv run pytest tests/test_config.py tests/test_generator.py
    tests/test_shutdown.py -q` → 76 passed.
  * Full suite `uv run pytest -q` → 988 passed, 4 pre-existing
    warnings (down from 991: three heartbeat tests deleted, no new
    failures).
  * `uv run ruff check src/ tests/` → all checks passed.
- Deployment note (PR body): `HEARE_HEARTBEAT_MIN=<N>` lines in `.env`
  files are now silently ignored — no warning, no error. Owners can
  drop the line at their convenience.

## US-WU-03 — implemented (2026-04-25)
- Plan picked Option A: delete CCS-03 dead code. The deadline-warning cue
  fired CONFIRMATION_DEADLINE 5s before the confirmation timeout. The
  implementation lived in dormant DeciderProcessor; production runs
  GeneratorProcessor, which has no AWAITING_CONFIRMATION state, so the
  cue had nowhere to fire. Feature deferred until a confirmation flow
  exists in the live pipeline.
- Deletions in `src/decider.py` (~30 lines):
  * `_deadline_warning_task: asyncio.Task | None = None` field on
    `DeciderProcessor.__init__` — gone.
  * `_deadline_warning_watcher` async method (with race-guard re-check
    against `_timeout_task`) — gone.
  * `_schedule_timeout_task` no longer schedules the parallel warning
    task. The original `_timeout_task = loop.create_task(...)` and the
    `_cancel_timeout_task()` precondition stay.
  * `_cancel_timeout_task` no longer cancels the warning task. The
    original `_timeout_task` clear-then-cancel sequence stays.
  * `from .indication import IndicationKind, get_indication` import
    deleted — no longer needed in decider.py.
- Deletion in `src/config.py`:
  * `confirmation_deadline_warning_seconds: float = 5.0` Setting field
    plus its CCS-03 comment block — gone. Nothing reads it.
- Deletion in `tests/test_decider.py`:
  * The 8 CCS-03 tests deleted (`test_deadline_warning_fires_at_T_minus_N`,
    `test_no_deadline_warning_when_user_confirms_early`,
    `test_no_deadline_warning_when_user_cancels_early`,
    `test_warning_disabled_when_warning_seconds_is_zero`,
    `test_warning_clamped_when_exceeds_timeout`,
    `test_existing_timeout_path_unchanged`,
    `test_warning_respects_indication_facade_gating`,
    `test_confirm_during_warning_fire_race_fuzz`).
  * The CCS-03 fixture (`ccs03_facade`), helpers (`_CCS03RecordingFacade`,
    `_ccs03_arm`), and CCS-03-only imports (`asyncio as
    _ccs03_asyncio`, `logging as _ccs03_logging`, `random as
    _ccs03_random`, `dataclass`/`field` aliases, `_ccs03_ind_mod`,
    `_CCS03_IndKind`) all deleted along with the tests they served.
    The `# US-CCS-03` section header replaced with a deletion-rationale
    comment.
- What was kept (intentional):
  * `IndicationKind.CONFIRMATION_DEADLINE` enum value in
    `src/indication.py` — zero callers, but cheap to keep and ready for
    a future confirmation flow inside GeneratorProcessor.
  * `Settings.confirmation_timeout_seconds` field — still used by
    `DeciderProcessor`'s primary `_timeout_watcher` path, which fires
    the "nevermind, cancelled" cleanup. US-WU-05 will delete the
    dormant `DeciderProcessor` outright and the field can ride along.
  * `_timeout_watcher` itself — unchanged. Only the parallel warning
    task is being removed.
- Annotation: `.omc/progress-ccs-completed.txt` US-CCS-03 section gained
  a one-line `> 2026-04-25 update (PRD WU)` note pointing at this story.
- Verification:
  * `uv run pytest tests/test_decider.py tests/test_config.py -q` → 121
    passed.
  * Full suite `uv run pytest -q` → 980 passed, 4 pre-existing warnings
    (down from 988: 8 CCS-03 tests deleted, no new failures).
  * `uv run ruff check src/decider.py src/config.py tests/test_decider.py`
    → all checks passed.
  * `grep -rn '_deadline_warning\|confirmation_deadline_warning_seconds'
    src/ tests/` → zero matches.
- Acceptance criteria from PRD: all 9 ACs verified — dead code deleted,
  setting deleted, tests deleted, enum value preserved, primary timeout
  path untouched, lint clean, full suite green, count drops by exactly
  8, CCS-completed annotation appended.
