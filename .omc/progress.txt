## heare realtime progress watch — implementation log

### RT-001: Schema v3 events table + EventKind + log_event + WAL + purge
- src/storage.py: bumped SCHEMA_VERSION from 2 to 3; added StrEnum-based EventKind with 14 members (decider.*, action.*, state.listening, system.emit_drops)
- src/storage.py SCHEMA: appended `events` table with id/ts/kind/transcript_id/decision_id/payload_json columns; FK(transcript_id)->transcripts; FK(decision_id)->decisions ON DELETE SET NULL; two indexes (idx_events_ts DESC, idx_events_decision on (decision_id, ts))
- src/storage.py init(): added `PRAGMA journal_mode=WAL` after connect (idempotent) so 0.5s watch polling doesn't contend with writes
- src/storage.py log_event(kind, *, transcript_id, decision_id, payload) async method inserting into events with time.time() ts; `str(kind)` works for both StrEnum and bare strings
- src/storage.py purge_older_than(days) now also DELETEs events WHERE ts < cutoff and sums both rowcounts
- tests/test_storage.py: updated test_schema_version_fail_loud_on_newer_db regex (was r"99.*2|2.*99", now r"99") so it's agnostic to current SCHEMA_VERSION
- tests/test_storage.py: +8 new tests (schema_version_v3_on_fresh_install, schema_upgrade_v2_to_v3 end-to-end via hand-built v2 db, log_event_happy_path, log_event_null_fks, log_event_accepts_strenum, purge_older_than_includes_events, wal_mode_set_after_init, events_indexes_exist)
- Test count: 310 baseline -> 318 (+8), ruff clean, zero regressions
- Key learning: aiosqlite.connect() ignores PRAGMA as a special case, so it must be issued via execute() like any other statement. The PRAGMA return value is the journal mode name ("wal"); on a fresh DB it will return "wal" because SQLite applies the pragma immediately.

### RT-002: Decider fire-and-forget emit queue + 12 state-transition sites
- src/decider.py: imports `EventKind` from .storage; adds `_EMIT_QUEUE_SIZE = 256` module constant (rationale: 10s action ~20 stdout + ~12 transitions = ~32 events, 8x headroom)
- DeciderProcessor.__init__ now constructs self._emit_queue (asyncio.Queue maxsize=256), self._emit_drop_count=0, self._emit_drainer_task=None
- New methods: _safe_emit (sync put_nowait with drop counter + WARNING once per 10 drops), _ensure_drainer (lazy task start if loop is running), _drain_events (infinite loop awaiting queue.get() + store.log_event, catches all exceptions and continues), shutdown (cancel drainer with 100ms wait_for grace)
- 12 emit sites wired:
  - _handle_listening: decider.start (before call_decider), decider.done (after log_decision), decider.dropped_low_conf (in confidence-floor branch), action.armed (in d_type=='act' branch)
  - _handle_confirmation: action.confirmed (yes), action.cancelled (no), action.reprompt (unclear)
  - _execute_pending: action.executing (after state=EXECUTING), action.call_start (before call_action), action.done (try branch), action.error (except branch), state.listening (finally)
  - _cancel_pending: state.listening added for completeness (since it also transitions to LISTENING)
- Principle 3 (fail-safe) verified: test_safe_emit_under_lock_is_fast measures 50 enqueues inside `async with self._lock` complete in <50ms total (well under 1ms each)
- tests/test_decider.py: +9 tests for RT-002 (queue overflow, drainer store-failure survival, lock timing, drainer cleanup, emit sequence for act-yes/act-no/act-error/dropped_low_conf) plus the RT-005 import canary
- Test count: 318 -> 327 (+9), ruff clean, zero regressions
- Key learning 1: `_ensure_drainer` must call `asyncio.get_running_loop()` and gracefully return if no loop — otherwise unit tests constructing DeciderProcessor outside an event loop would crash. In practice the first _safe_emit happens inside an async test function so this works.
- Key learning 2: The existing test_golden_string_flag_off_render uses a fixed-ctx dict (not builder.build()) to avoid wall-clock drift. First attempt of RT-005 used builder.build() and failed with a timestamp mismatch — copying the fixed-ctx pattern resolved it. The decider import canary is now byte-identical to the golden and catches any future import-time drift.

### RT-005: Flag-off decider-import canary (shipped with RT-002)
- tests/test_decider.py::test_decider_import_does_not_break_flag_off_render explicitly `import src.decider` at function top to exercise import-time side effects of the new EventKind reference and emit infrastructure
- Uses the same fixed-ctx dict as test_context.py::test_golden_string_flag_off_render so wall-clock timestamp drift is eliminated
- Confirms zero drift after RT-001 (EventKind added) and RT-002 (queue + emitters)

### RT-003: ClaudeCLI._run streaming refactor + action.stdout emission
- src/claude_cli.py: _run gains on_line callback param; replaces proc.communicate() with await asyncio.wait_for(self._read_streams(proc, on_line), timeout=self.timeout)
- New _read_streams method: concurrent drain_stdout + drain_stderr inner coroutines via asyncio.gather, then proc.wait(), returns stripped (stdout, stderr) tuple for backward-compat with _log_invocation
- drain_stdout uses proc.stdout.readline() in a loop; catches asyncio.LimitOverrunError and falls back to proc.stdout.read(8192); splits decoded bytes on BOTH \r AND \n via .replace("\r","\n").split("\n") so progress-bar tools (pytest -v, pip, npm) render as visible motion; invokes on_line per non-empty segment in try/except that logs WARNING on callback exceptions
- call_action plumbed with on_line kwarg to _run; call_decider unchanged (no streaming for JSON decider output)
- src/decider.py _execute_pending: closure _stdout_emit calls self._safe_emit(ACTION_STDOUT, decision_id, payload={'line': line[:8192]}) and passes it to call_action(on_line=_stdout_emit); 8KB line cap protects the queue from runaway single lines
- tests/test_claude_cli.py: rewrote _make_mock_proc helper to support BOTH legacy communicate() path AND readline path. Critical pattern: side_effect=list exhausts on retry reuse (StopAsyncIteration), so switched to cursor-closure async function that returns b'' forever after exhausting the queue. Test test_run_timeout_kills_process: asyncio.sleep is globally patched by existing fixtures so sleep-based hangs return instantly; use asyncio.Event().wait() instead which routes through loop.create_future, unaffected by sleep patching
- +5 RT-003 claude_cli tests: streams_stdout_via_on_line (3 newline-separated lines), splits_on_cr_for_progress_bars ('a\rb\rc\n' -> 3 segments), call_action_plumbs_on_line (end-to-end via call_action), stderr_drained_in_parallel (both streams), on_line_callback_exception_does_not_break_stream (raising callback doesn't halt reading)
- +1 RT-003 decider test: emits_action_stdout_per_line (fake call_action side_effect invokes on_line 3 times; events table has 3 action.stdout rows with correlated decision_id and JSON payloads)
- Test count: 327 -> 333 (+6), ruff clean, zero regressions
- Key learning: `AsyncMock(side_effect=list)` is unusable for readline mocks that must survive retry reuse — use a cursor-closure that persists across calls and returns EOF sentinel forever. Direct-assignment of async functions is also cleaner than AsyncMock wrapping for tests that want guaranteed behavior.

### RT-004: Watch progress panel + layout rewire + 0.5s refresh default
- src/watch.py: new _progress_panel(con) queries last 8 events via SELECT ts, kind, decision_id, payload_json FROM events ORDER BY ts DESC LIMIT 8; reverses for chronological display
- Color families: *.error=red, decider.*=cyan, action.stdout=white, action.*=yellow, state.*=dim
- Payload preview extracted from JSON via common keys (line, intent, summary, type, error, reason) — shows the first non-empty one, truncated to 60 chars
- action.stdout rows get a "│ " prefix so they visually indent under the parent action row
- Handles con=None with "(no progress yet)" placeholder
- _build_layout rewired: header(size=4) + body(ratio=1) + progress(size=10) + log(size=8 with lines=6 content) — row budget: 22 fixed rows total, body gets remaining terminal height (18 on 40-row term), minimum viable 32-row terminal
- src/main.py: watch subparser default --interval 2.0 -> 0.5 (WAL mode from RT-001 makes 4x read load safe)
- tests/test_watch.py: +6 tests using a _rich_text helper that flattens Panels via rich.console.Console(record=True).export_text() for substring assertions. Color-coding test uses `panel.renderable` (which is a Group) and walks `.renderables` extracting Text.style values. Default-interval test via build_parser().parse_args(['watch']).interval == 0.5
- Test count: 333 -> 339 (+6), ruff clean, zero regressions

### Phase totals
- Starting baseline: 310 tests
- Final: 339 tests (+29 across RT-001 through RT-005)
- Ruff: clean throughout every story
- Flag-off golden fixture: byte-identical (canary test verifies after each story)
- Files modified: src/storage.py, src/decider.py, src/claude_cli.py, src/watch.py, src/main.py, tests/test_storage.py, tests/test_decider.py, tests/test_claude_cli.py, tests/test_watch.py
- Files created: none (all changes extend existing files)


## heare daemon polish (Phase A) — follow-up to RT review

### DP-001: Commit RT-001..RT-005 realtime-progress-watch work
- Staged explicit file list (9 src+tests + .omc/progress.txt + .omc/project-memory.json + .omc/prd-speaker-phase-2-track-b-completed.json) and committed with `feat(watch):` prefix; commit hash bfb8675
- Excluded .omc/prd.json (Phase A PRD, rides in DP-005) and .omc/prd-realtime-progress-watch-completed.json (archive, rides in DP-005)
- Sanity re-run of the 145 RT-touched tests post-commit: all pass

### DP-002: Wire DeciderProcessor.shutdown() into pipeline teardown
- src/main.py run_until_stopped: added keyword-only `decider=None` parameter; finally-block awaits `decider.shutdown()` after the named_tasks cancel+await loop and before signal-handler removal, wrapped in try/except that logs exceptions so a broken shutdown cannot break teardown
- src/main.py _cmd_start: passes `decider=decider` from build_pipeline tuple into run_until_stopped
- tests/test_shutdown.py: +2 tests (test_run_until_stopped_awaits_decider_shutdown with FakeDecider counting shutdown_called, test_run_until_stopped_survives_decider_shutdown_failure with FailingDecider raising RuntimeError)
- Existing test_run_until_stopped_cancels_on_sigterm and test_run_until_stopped_cancels_when_pipeline_exits pass unchanged — keyword-only default-None parameter is backward compatible

### DP-003: PRAGMA foreign_keys=ON + ON DELETE SET NULL cascade
- src/storage.py TranscriptStore.init(): added `PRAGMA foreign_keys=ON` on the line after WAL pragma, before executescript(SCHEMA)
- tests/test_storage.py: +2 tests (test_foreign_keys_enabled_after_init, test_event_decision_id_cascade_sets_null which inserts transcript->decision->event chain, DELETEs the decision, asserts event.decision_id is now NULL via the ON DELETE SET NULL cascade)
- Fix-up: test_log_event_happy_path was passing transcript_id=5 (hardcoded sentinel that never existed). FK enforcement broke it instantly. Updated to insert a real transcript via log_transcript first and reference its returned id — this is actually a correctness improvement, not a workaround
- Key learning: turning on SQLite foreign_keys is a one-way door for the test suite. Any prior test that used sentinel FK ids was silently getting away with it. Grep'd for `transcript_id=\\d|decision_id=\\d` across tests/ — only the one broken site found

### DP-004: Fix proc.kill mock warning in test_claude_cli
- tests/test_claude_cli.py: imported MagicMock alongside AsyncMock/patch; _make_mock_proc sets `proc.kill = MagicMock()` (sync) instead of AsyncMock; docstring updated to explain why (real Popen.kill is sync, AsyncMock leaks an unawaited coroutine in the timeout path)
- test_run_timeout_kills_process still green with `p.kill.assert_called()` — MagicMock supports the same assertion surface
- Pre-fix: 1 RuntimeWarning in test_run_timeout_kills_process. Post-fix: zero warnings

### DP-005: Commit Phase A polish + archive RT PRD
- .omc/prd-realtime-progress-watch-completed.json: byte-identical snapshot of the RT PRD taken via `cp .omc/prd.json ...` BEFORE the Phase A PRD was written
- .omc/prd.json: rewritten with 5 Phase A stories (DP-001..DP-005), all marked passes: true after verification
- Second commit on HEAD: src/main.py + src/storage.py + tests/test_claude_cli.py + tests/test_shutdown.py + tests/test_storage.py + .omc/prd.json + .omc/prd-realtime-progress-watch-completed.json + .omc/progress.txt

### Phase A totals
- Starting baseline: 339 tests (RT phase final)
- Final: 343 tests (+4: 2 shutdown tests + 2 FK tests)
- Ruff: clean
- Zero regressions after fixing the transcript_id=5 sentinel in test_log_event_happy_path
- Files modified: src/storage.py, src/main.py, tests/test_storage.py, tests/test_shutdown.py, tests/test_claude_cli.py
- Files created: .omc/prd-realtime-progress-watch-completed.json (RT PRD archive)
- Commit 1: bfb8675 `feat(watch): ...` (RT phase)
- Commit 2: 73c7bc3 `chore(daemon): wire shutdown, enable FKs, fix kill mock` (Phase A)
- Commit 3: efe3a18 `chore(tests): trim task-id references from new DP test docstrings` (deslop)


## heare speaker recognition Phase 2 Track A

Phase 2 Track A shipped all four remaining stories. Gallery growth (A0)
was already wired in Phase 2 Track B (speaker_processor.py:467-480 calls
register_session_ref on every match and append_reference on high-confidence
matches) so it needed no new work.

### SPK-A1: Context redaction — non-owner transcripts hidden from decider prompt
- src/storage.py recent_transcripts SELECT extended to include speaker_id; return dicts now carry a speaker_id key (may be None for legacy rows)
- src/context.py _format_recent learns about settings.speaker_id_enabled: when ON, rows with speaker_id != 'owner' (including None as fail-closed) render their text as literal '[інший голос]'; when OFF, verbatim pass-through
- Flag-off golden fixture byte-identical. RT-005 decider-import canary byte-identical
- tests/test_storage.py: +1 test (recent_transcripts_includes_speaker_id)
- tests/test_context.py: +3 tests (redacts_non_owner_when_flag_on, passthrough_when_flag_off, none_speaker_id_treated_as_non_owner)
- Fix-up: pre-existing test_recent_transcripts_rendering was dev-config dependent because load_settings() reads ~/.heare/config.toml where speaker_id_enabled was True on this machine. Made it explicit: `settings.speaker_id_enabled = False`
- Key learning: load_settings() is NOT hermetic — it reads the dev user's config.toml. Any new test that depends on a specific flag value must set it explicitly instead of trusting the default. The existing test_main_cli.py pattern of constructing Settings() directly is the better pattern

### SPK-A2: Speakers CLI — list / info / rm / rename
- src/speaker_gallery.py: new remove_speaker + rename_speaker + get_entry methods
- src/main.py: new `speakers` subparser with list/info/rm/rename/audit sub-subcommands; five _cmd_speakers_* handlers + a dispatcher _cmd_speakers. rm requires --yes to confirm; rename traps LabelValidationError and returns 1 on bad labels
- tests/test_speaker_gallery.py: +5 tests for remove/rename (success, missing, invalid label)
- tests/test_main_cli.py: +8 tests for list_empty/list_prints_all/info_prints_fields/info_missing/rm_requires_yes/rm_with_yes/rename_success/rename_invalid_label — all use `patch("src.main.load_settings", return_value=Settings(...))` to stay hermetic
- Key learning: the existing test_main_cli pattern creates a Settings() directly and patches load_settings via unittest.mock — no conftest needed

### SPK-A3: Drift audit — per-speaker cosine statistics
- src/speaker_gallery.py: new audit(speaker_id) method returning {ref_count, min/mean/max cosine vs centroid, mean cosine vs enrollment ref, enrollment_cos_floor_hit}. Uses the same numpy vector ops as identify()/get_centroid()
- Drift threshold reuses AUTO_ENROLL_MIN_COS (0.55) — keeps the floor in one place
- src/main.py: _cmd_speakers_audit formats per-speaker OK/DRIFT report; positional speaker_id is optional (defaults to auditing everything)
- tests/test_speaker_gallery.py: +4 tests (single_ref, multi_ref_healthy via near-identical appended refs, detects_drift by directly mutating gallery._speakers to bypass append_reference's anti-drift guard, missing_returns_none)
- tests/test_main_cli.py: +2 tests (prints_ok_for_healthy, prints_drift_marker via direct mutation then save())
- Key learning: to test drift detection, bypass append_reference and mutate _speakers directly — the public API refuses to create a drifted gallery on purpose

### SPK-A4: Auto-enrollment of repeated non-owner voices (flag-gated, default off)
- src/config.py: new `speaker_id_auto_enroll_enabled: bool = False` — separate master switch from speaker_id_enabled so existing deployments and tests are unaffected
- src/speaker_gallery.py: new enroll_guest(v, max_guests=99) method. Scans existing guest_NN slots, picks the lowest free N, inserts a gallery entry with label='guest_NN', persists. Max 99 guests total — returns empty string + logs WARNING on overflow to protect against noisy environments
- src/speaker_processor.py: SpeakerTaggerProcessor gains self._stranger_candidates (deque maxlen=10). Cleared on BotStartedSpeakingFrame so own-voice echo can't poison the cluster. New _maybe_auto_enroll(embedding) helper: count how many prior candidates have cosine >= speaker_id_threshold_match against the new embedding; if (matches+1) >= speaker_id_auto_enroll_after (default 3), call gallery.enroll_guest and clear the buffer on success
- Wired into the non-owner branch of _tag_transcription only when the flag is on AND slot.embedding is not None AND slot.duration_ms >= min_duration_ms
- tests/test_speaker_gallery.py: +3 tests (enroll_guest_assigns_next_id, enroll_guest_persists, enroll_guest_max_cap via max_guests=2 parameter override)
- tests/test_speaker_processor.py: +5 tests using monkeypatch.setattr(sid, 'embed', ...) to return a stranger vector per turn. New _run_stranger_turn helper encapsulates the start/audio/stop/transcript cycle. Tests cover: disabled_by_default (5 turns, no guest), triggers_after_threshold (3 matching turns, guest_01 enrolled), ignores_dissimilar_candidates (rotating orthogonal embeddings, no enrollment), ignores_short_turns (200ms audio, never reaches auto-enroll branch), clears_buffer_after_success (_stranger_candidates empty after enroll)
- Security invariant: non-owner speakers are still blocked from command authority by the existing decider drop at decider.py:524. Auto-enrollment only stabilizes stranger labels for context redaction and operator visibility — it does NOT grant any new permissions. This keeps the fail-closed model intact
- Key learning: to test the 'matching embeddings' path, just return the SAME vector from embed on every call (cosine=1.0). To test the 'dissimilar' path, rotate through orthogonal standard-basis vectors (cosine=0.0). Both patterns avoid the speechbrain/torch dependency entirely

### Track A totals
- Starting baseline: 343 tests (Phase A daemon-polish final)
- Final: 374 tests (+31 across SPK-A1..A4)
- Ruff: clean throughout
- Flag-off golden fixture: byte-identical after each story
- Files modified: src/storage.py, src/context.py, src/config.py, src/main.py, src/speaker_gallery.py, src/speaker_processor.py, tests/test_storage.py, tests/test_context.py, tests/test_speaker_gallery.py, tests/test_speaker_processor.py, tests/test_main_cli.py
- Files created: .omc/prd-daemon-polish-completed.json (Phase A archive)


