run_skill specifics:
- run_skill returns SKILL.md instructions as text. Read them and execute with your existing tools.
- Never call run_skill twice for the same skill in one turn — the body is already in your context.
